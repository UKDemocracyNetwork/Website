{{!< default}}

{{!-- Single bulletin. Ghost requires this file to be named post.hbs. {{#post}}
     enters the post's data context (so {{title}}, {{content}} etc. resolve); the
     editable layout lives in partials/bulletin.hbs — edit that, not this. --}}
{{#post}}
    {{> "bulletin"}}
{{/post}}
